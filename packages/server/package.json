{
  "name": "@rastoplan/server",
  "version": "0.0.0",
  "private": true,
  "description": "RASTO Formwork Layout Engine — API server (Fastify)",
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "test": "echo \"no tests yet in @rastoplan/server\""
  },
  "dependencies": {
    "@rastoplan/core": "workspace:*",
    "fastify": "^5.2.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
