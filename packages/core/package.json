{
  "name": "@rastoplan/core",
  "version": "0.0.0",
  "private": true,
  "description": "RASTO Formwork Layout Engine — pure TypeScript calculation core (isomorphic, no DOM/Node APIs)",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
